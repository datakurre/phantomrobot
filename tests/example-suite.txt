*** Settings ***
Library  Remote  http://localhost:1337/

Suite Setup  Start browser
Suite Teardown  Close browser

*** Test cases ***

As a sci-fi fan want to find the trailer for Iron Sky
    Given I can google
    When I google for  Iron Sky
    and I click the first result
    Then I get to a page with the trailer

*** Keywords ***

Start browser
    Open browser  http://www.google.com/

I can google
    Go to  http://www.google.com/

I google for
    [Arguments]  ${something}
    Input text  xpath=//input[@name='q']  ${something}
    Submit form  xpath=//form[@action='/search']

I click the first result
    Click link  css=h3 a

I get to a page with the trailer
    Page should contain  xpath=//title[contains(text(), 'Theatrical Trailer')]
    Capture Page Screenshot
